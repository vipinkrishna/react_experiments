import React from "react";

export class Angular {
  static io(parameter) {
    return "Angular.io " + parameter;
  }
}

export const Print = (parameter = "Give some value.") => (parameter);

export const Output = () => ("Java programming");


export default Angular;